pipeline {
    agent any
    stages {
        stage('Test Trigger') {
            steps {
                echo 'Pipeline triggered successfully!'
                sh 'kubectl get nodes'
            }
        }
    }
}
